<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="12dp"
    android:background="@null">

    <ImageView
        android:id="@+id/imageView2"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:contentDescription="@string/answer_message"
        app:srcCompat="@drawable/ic_bot_image"
        android:layout_marginStart="12dp"
        android:layout_marginTop="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/tv_bot_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="212dp"
        android:text="@string/dummy_bot_message"
        android:background="@drawable/background_bot_message"
        android:layout_marginStart="12dp"
        android:fontFamily="@font/anekdevanagari_regular"
        android:textSize="12sp"
        android:textColor="@color/white_bot_message"
        android:padding="8dp"
        android:paddingStart="10dp"
        android:paddingEnd="8dp"
        app:layout_constraintTop_toTopOf="@id/imageView2"
        app:layout_constraintStart_toEndOf="@id/imageView2"/>


</androidx.constraintlayout.widget.ConstraintLayout>